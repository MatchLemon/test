git test, I have two branch;
git branch branchname,git checkout -b branch name
git pull
git push
git remote add 
git remote update
git remote rm
git remote 
git fetch
git reset -mixed 回退commit不回退源码
git reset -hard 回退全部
git rsset HEAD^ 回退到上一个版本