git test, I have two branch;
git branch branchname,git checkout -b branch name
git pull
git push
git remote add 
git remote update
git remote rm
git remote 
git fetch
git reset -mixed 回退commit不回退源码
git reset -hard 回退全部
git rsset HEAD^ 回退到上一个版本
git checkout -- . 暂存区域恢复到工作区域
git reset HEAD 从版本库恢复到暂存区域